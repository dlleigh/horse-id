services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: horseid
      POSTGRES_PASSWORD: horseid
      POSTGRES_DB: horseid
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
